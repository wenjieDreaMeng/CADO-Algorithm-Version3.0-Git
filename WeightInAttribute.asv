function [ Entropy ] = WeightInAttribute( Data )

%   Function:   该函数求出每一列熵作为内耦合权重值
%   Input:      数据集Data
%   Output:     权重矩阵
[row,col] = size(Data);

Entropy = zeros(1,col);
for i = 1:col
    Element = unique(Data(:,))


end